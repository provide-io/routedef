[project]
name = "routedef-cloudflare-worker"
version = "0.1.0"
description = "Cloudflare Python Worker integration fixture for routedef."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
